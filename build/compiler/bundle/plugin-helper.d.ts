import * as d from '../../declarations';
export declare const pluginHelper: (config: d.Config, builtCtx: d.BuildCtx) => {
    name: string;
    resolveId(importee: string, importer: string): null;
};
